// Example JavaScript file to test Misogi Copilot
// Try the following:
// 1. Configure your OpenAI API key using Cmd+Shift+P -> "Misogi Copilot: Configure API Key"
// 2. Start typing code and see inline completions
// 3. Select some code and use Cmd+Shift+S for suggestions
// 4. Enable hover help in settings and hover over functions

function calculateSum(a, b) {
    // Try typing here - the AI will suggest completions
    
}

function fibonacci(n) {
    // Select this function and ask for suggestions
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// Try typing: "function sort" and see what the AI suggests